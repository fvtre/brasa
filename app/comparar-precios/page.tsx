import { BadgeCheck, Truck, TrendingDown } from "lucide-react"
import { bestRows } from "@/lib/price-intelligence"
import { formatCLP } from "@/lib/format"

export default function CompararPreciosPage(){const groups=bestRows();return <div className="mx-auto max-w-6xl px-4 py-10"><p className="text-sm font-semibold text-primary">Price Intelligence · demo</p><h1 className="mt-1 text-3xl font-extrabold">Compara productos e insumos</h1><p className="mt-2 max-w-2xl text-muted-foreground">Brasa compara precio y despacho para destacar el menor costo total. Los datos actuales son demostrativos y la arquitectura queda lista para catálogos/APIs.</p><div className="mt-8 grid gap-5 lg:grid-cols-2">{groups.map(g=><section key={g.product} className="rounded-2xl border bg-card p-5"><h2 className="text-lg font-bold">{g.product}</h2><div className="mt-4 space-y-2">{g.rows.map((r,i)=><div key={r.provider} className={`rounded-xl border p-4 ${i===0?"border-primary/40 bg-primary/5":""}`}><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><b>{r.provider}</b>{i===0&&<span className="inline-flex items-center gap-1 rounded-full bg-primary/10 px-2 py-0.5 text-xs font-semibold text-primary"><BadgeCheck size={12}/> Mejor costo total</span>}</div><p className="mt-1 text-xs text-muted-foreground"><Truck size={12} className="mr-1 inline"/>Despacho {r.delivery?formatCLP(r.delivery):"gratis"}</p></div><div className="text-right"><p className="font-bold">{formatCLP(r.price)}/{r.unit}</p><p className="text-xs text-muted-foreground">Total base: {formatCLP(r.price+r.delivery)}</p></div></div></div>)}</div></section>)}</div><div className="mt-8 rounded-2xl border border-primary/30 bg-primary/10 p-5"><TrendingDown className="size-5 text-primary"/><h2 className="mt-2 font-bold">Optimización multi-proveedor</h2><p className="mt-1 text-sm text-muted-foreground">La siguiente iteración calculará una canasta completa y decidirá si conviene comprar todo en un proveedor o dividir la compra para reducir el costo total.</p></div></div>}
