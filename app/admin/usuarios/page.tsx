import { requireRole } from '@/lib/auth'
import { createClient } from '@/lib/supabase/server'
import { Card,CardContent,CardHeader,CardTitle } from '@/components/ui/card'
export default async function AdminUsers(){await requireRole(['administrador']);const supabase=await createClient();const {data:rows}=await supabase.from('profiles').select('*').order('created_at',{ascending:false});return <div className="mx-auto max-w-6xl px-4 py-10"><p className="text-sm font-semibold text-primary">Administración</p><h1 className="mt-1 text-3xl font-extrabold">Usuarios</h1><Card className="mt-8"><CardHeader><CardTitle>{rows?.length||0} cuentas</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b text-left text-muted-foreground"><th className="py-3">Nombre</th><th>Email</th><th>Rol</th><th>Estado</th><th>Creado</th></tr></thead><tbody>{(rows||[]).map((u:any)=><tr className="border-b" key={u.id}><td className="py-3 font-medium">{u.full_name||'Sin nombre'}</td><td>{u.email}</td><td className="capitalize">{u.role}</td><td>{u.active?'Activo':'Inactivo'}</td><td>{new Date(u.created_at).toLocaleDateString('es-CL')}</td></tr>)}</tbody></table></CardContent></Card></div>}
